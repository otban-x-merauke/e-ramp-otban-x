name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare website with institutional logos, English SI guidance, formal record, and auth redirect fix
        run: |
          python - <<'PY'
          from pathlib import Path
          import base64
          import struct

          site = Path('_site')
          site.mkdir(exist_ok=True)

          def decode_b64_file(path):
              value = ''.join(Path(path).read_text().split())
              value += '=' * (-len(value) % 4)
              return base64.b64decode(value)

          kemenhub_data = decode_b64_file('assets/logo-kemenhub.b64')
          if not kemenhub_data.startswith(b'\x89PNG\r\n\x1a\n'):
              raise SystemExit('Logo Kemenhub bukan file PNG yang valid.')
          width, height = struct.unpack('>II', kemenhub_data[16:24])
          if width < 100 or height < 100:
              raise SystemExit('Ukuran logo Kemenhub tidak valid.')
          (site / 'logo-kemenhub-final.png').write_bytes(kemenhub_data)

          (site / 'logo-otban-x.jpg').write_bytes(
              decode_b64_file('assets/logo-otban-x.b64')
          )

          website_files = (
              'si-guide-a.js',
              'si-guide-b.js',
              'si-guide-cde.js',
              'si-guide-en.js',
              'si-guide-ui.js',
              'auth-redirect-fix.js',
              'formal-ramp-form.js',
              'learning.html',
              'login-notifications.js',
              
          )
          for filename in website_files:
              source = Path(filename)
              if not source.exists():
                  raise SystemExit(f'File website tidak ditemukan: {filename}')
              (site / filename).write_text(source.read_text(encoding='utf-8'), encoding='utf-8')

          html = Path('index.html').read_text(encoding='utf-8')

          css = r'''
          .brand-header{display:flex;align-items:center;gap:16px}
          .header-logo-group,.nav-logo-group{display:flex;align-items:center;gap:8px;flex-shrink:0}
          .header-logo-group img,.nav-logo-group img{display:block;object-fit:contain;object-position:center}
          .header-logo-group .logo-kemenhub{height:64px;width:auto;max-width:64px;background:transparent;padding:0;border:0;border-radius:0;box-shadow:none}
          .header-logo-group .logo-otban{width:60px;height:60px;background:#071826;padding:3px;border-radius:14px;border:2px solid rgba(255,255,255,.9);box-shadow:0 5px 14px rgba(0,0,0,.2)}
          .header-copy h1{margin:0;font-size:22px}.header-copy small{display:block;margin-top:3px}
          .nav-institution{display:flex;align-items:center;gap:10px;margin:3px 5px 12px;padding:10px;border-radius:12px;background:linear-gradient(135deg,#eef5ff,#fff);border:1px solid #d7e1ee}
          .nav-logo-group{width:auto}
          .nav-logo-group .logo-kemenhub{height:46px;width:auto;max-width:46px;background:transparent;padding:0;border:0;border-radius:0;box-shadow:none}
          .nav-logo-group .logo-otban{width:43px;height:43px;background:#071826;padding:2px;border-radius:10px;border:1px solid #d7e1ee;box-shadow:0 3px 9px rgba(18,46,88,.12)}
          .nav-institution strong{display:block;font-size:12px;color:#092b62}.nav-institution span{display:block;font-size:10px;color:#68758a;margin-top:2px}
          @media(max-width:800px){.brand-header{align-items:flex-start}.header-logo-group .logo-kemenhub{height:52px;max-width:52px}.header-logo-group .logo-otban{width:49px;height:49px}.header-copy h1{font-size:19px}.nav-institution{grid-column:1/-1}}
          '''
          html = html.replace('</head>', f'<style>{css}</style></head>')

          old_header = '<header><h1>E-RAMP OTBAN X</h1><small>Digital Ramp Inspection Checklist • Kantor Otoritas Bandar Udara Wilayah X Merauke</small></header>'
          new_header = '''<header class="brand-header">
          <div class="header-logo-group" aria-label="Logo instansi">
            <img class="logo-kemenhub" src="logo-kemenhub-final.png?v=8" alt="Logo Kementerian Perhubungan" width="55" height="64">
            <img class="logo-otban" src="logo-otban-x.jpg?v=8" alt="Logo Otoritas Bandar Udara Wilayah X Merauke">
          </div>
          <div class="header-copy"><h1>E-RAMP OTBAN X</h1><small>Digital Ramp Inspection Checklist • Kantor Otoritas Bandar Udara Wilayah X Merauke</small></div>
          </header>'''
          if old_header not in html:
              raise SystemExit('Header website tidak ditemukan; deployment dihentikan agar file tidak rusak.')
          html = html.replace(old_header, new_header, 1)

          old_nav = '<nav>\n<div id="userbar" class="userbar">Belum login</div>'
          new_nav = '''<nav>
          <div class="nav-institution">
            <div class="nav-logo-group"><img class="logo-kemenhub" src="logo-kemenhub-final.png?v=8" alt="Kemenhub" width="40" height="46"><img class="logo-otban" src="logo-otban-x.jpg?v=8" alt="Otban X"></div>
            <div><strong>OTBAN WILAYAH X</strong><span>Merauke</span></div>
          </div>
          <div id="userbar" class="userbar">Belum login</div>'''
          if old_nav not in html:
              raise SystemExit('Navigasi website tidak ditemukan; deployment dihentikan agar file tidak rusak.')
          html = html.replace(old_nav, new_nav, 1)

          scripts = '''
          <script src="si-guide-a.js"></script>
          <script src="si-guide-b.js"></script>
          <script src="si-guide-cde.js"></script>
          <script src="si-guide-en.js"></script>
          <script src="si-guide-ui.js"></script>
          <script src="auth-redirect-fix.js"></script>
          <script src="formal-ramp-form.js"></script>
          <script src="login-notifications.js?v=1"></script>
          '''
          if '</body>' not in html:
              raise SystemExit('Tag penutup body tidak ditemukan.')
          html = html.replace('</body>', scripts + '</body>', 1)

          (site / 'index.html').write_text(html, encoding='utf-8')
          PY

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
